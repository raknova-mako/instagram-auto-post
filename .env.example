# .env.example をコピーして .env という名前で保存し、値を入れてください。
# .env は .gitignore 済みなので、GitHubには絶対に上がりません。

# Meta for Developers のダッシュボードで発行した長期アクセストークン
IG_ACCESS_TOKEN=ここに貼る

# Gemini APIキー（原稿生成用。あとで使うので今は空でOK）
GEMINI_API_KEY=
